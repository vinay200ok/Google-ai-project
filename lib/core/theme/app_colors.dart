import 'package:flutter/material.dart';

class AppColors {
  // Primary — Electric Blue
  static const Color primary = Color(0xFF0EA5E9);
  static const Color primaryDark = Color(0xFF0284C7);
  static const Color primaryLight = Color(0xFF38BDF8);

  // Secondary — Neon Green
  static const Color secondary = Color(0xFF22C55E);
  static const Color secondaryDark = Color(0xFF16A34A);

  // Accent — Amber
  static const Color accent = Color(0xFFF59E0B);
  static const Color accentOrange = Color(0xFFF97316);

  // Purple
  static const Color purple = Color(0xFF7C3AED);
  static const Color purpleLight = Color(0xFFA78BFA);

  // Backgrounds
  static const Color background = Color(0xFF060A14);
  static const Color surface = Color(0xFF0D1424);
  static const Color surfaceVariant = Color(0xFF141D2E);
  static const Color card = Color(0xFF1A2436);
  static const Color cardHighlight = Color(0xFF1E2D44);

  // Text
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textTertiary = Color(0xFF475569);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF0EA5E9);

  // Crowd density
  static const Color densityLow = Color(0xFF22C55E);
  static const Color densityMedium = Color(0xFFF59E0B);
  static const Color densityHigh = Color(0xFFF97316);
  static const Color densityCritical = Color(0xFFEF4444);

  // Borders & Glass
  static const Color border = Color(0xFF1E293B);
  static const Color borderHighlight = Color(0xFF0EA5E9);
  static const Color glassFill = Color(0x1A0EA5E9);
  static const Color glassBorder = Color(0x330EA5E9);

  // Gradients
  static const List<Color> primaryGradient = [Color(0xFF0EA5E9), Color(0xFF7C3AED)];
  static const List<Color> heroGradient = [Color(0xFF060A14), Color(0xFF0D1424), Color(0xFF1A2436)];
  static const List<Color> greenGradient = [Color(0xFF22C55E), Color(0xFF0EA5E9)];
  static const List<Color> fireGradient = [Color(0xFFF59E0B), Color(0xFFF97316)];
  static const List<Color> purpleGradient = [Color(0xFF7C3AED), Color(0xFF0EA5E9)];
}
